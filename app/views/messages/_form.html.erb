<%= form_with(model: message, local: true, html: { multipart: true }) do |form| %>
  

  <%= form.text_field :body, placeholder: "Type your message here..." %>

  <%= form.file_field :image %>

  <%= form.hidden_field :room_id, value: room.id %>

<% end %>